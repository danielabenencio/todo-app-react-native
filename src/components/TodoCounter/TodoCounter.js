import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    todoCounter:{
        container: {
            alignItems: 'center',
            padding: 24,
        },
        text: {
            fontSize: 30,
            textAlign: 'center',
        },
        bold: {
            fontWeight: 'bold',
        },
    }
});

export default styles;